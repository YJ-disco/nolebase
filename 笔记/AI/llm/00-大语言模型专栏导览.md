---
tags:
  - AI/llm
---

# 大语言模型专栏导览

本专栏收录大语言模型**自身**的知识：架构怎么演进、输出怎么采样、输入怎么切分。

和模型交互的那一层（提示怎么写、上下文怎么组装、Harness 怎么搭）在 `../agent/`，从 [[17-Prompt Engineering|Prompt Engineering]] 起。

## 目录

- 01 · [[01-语言模型演进：N-gram 到 RNN|语言模型演进：N-gram 到 RNN]] —— 链式法则与马尔可夫假设、极大似然估计、词嵌入与余弦相似度、RNN 的隐藏状态与长期依赖问题、LSTM 门控
- 02 · [[02-Transformer 架构|Transformer 架构]] —— Encoder-Decoder、自注意力的 Q/K/V 与缩放点积、多头注意力、逐位置前馈、残差连接与层归一化、位置编码
- 03 · [[03-Decoder-Only 与自回归|Decoder-Only 与自回归]] —— 为什么砍掉编码器、因果掩码怎么阻止「偷看」、三条优势
- 04 · [[04-采样参数|采样参数]] —— Temperature、Top-k、Top-p 的原理、取值区间与三者的优先级关系
- 05 · [[05-文本分词与子词算法：BPE、WordPiece 与 Unigram|文本分词与子词算法：BPE、WordPiece 与 Unigram]] —— 三层概念的区分（算法 / 工具 / 粒度）、BPE 与 byte-level BPE、WordPiece / Unigram / SentencePiece / tiktoken 的现状对照、分词器对上下文窗口与成本的影响、生产里的五个坑
- 06 · [[06-缩放法则|缩放法则]] —— 损失与参数量/数据量/计算量的幂律关系、Chinchilla 定律的最优配比修正、能力涌现的阈值效应
- 07 · [[07-模型幻觉|模型幻觉]] —— 事实性/忠实性/内在三类幻觉、三个成因、数据层与模型层与推理生成层三层缓解手段
- 08 · [[08-模型选型|模型选型]] —— 八个考量维度、API 与本地部署的取舍、与缩放法则和幻觉缓解的衔接
- 09 · [[09-位置编码|位置编码]] —— 绝对（正弦余弦 / 学习式）→ 相对 → RoPE 的旋转与 base 旋钮 → 长上下文外推（位置插值 / NTK-aware / YaRN）→ ALiBi；以及「哪种位置编码更好」为什么会因评测指标而翻转
- 10 · [[10-KV Cache 与推理优化|KV Cache 与推理优化]] —— 为什么能缓存、Prefill 与 Decode 的两套瓶颈、显存公式与各模型对照、MHA / GQA / MQA、PagedAttention 与 APC、五个坑、多轮缓存的失效边界
- 11 · [[11-Self-Attention 机制|Self-Attention 机制]] —— 从 Bahdanau 到点积的演进、QKV 的信息检索直觉、五步计算与维度全程跟踪、$\sqrt{d_k}$ 的方差证明、Online Softmax、多头参数量 $4d^2$、MHA→MQA→GQA→MLA、Causal Mask、FLOPs 分解
- 12 · [[12-FFN 与激活函数|FFN 与激活函数]] —— FFN 的 token 内运算定位与「知识库」角色、展开-压缩结构、ReLU / GELU / Swish 的对照、SwiGLU 的门控机制、参数量守恒推导、$d_{ff}$ 为什么是 4× 与 8/3×、Megatron 的列切/行切、MoE
- 13 · [[13-归一化与残差连接|归一化与残差连接]] —— 梯度消失与退化问题、残差连接的恒等通道证明、BatchNorm 为何不适合 NLP、LayerNorm 与 RMSNorm 的取舍、Pre-Norm vs Post-Norm 的梯度流分析与实验证据、LayerNorm kernel 的两遍扫描
- 14 · [[14-长上下文技术|长上下文技术]] —— $O(n^2)$ 的两笔账（分数矩阵 vs KV Cache）、稀疏注意力（滑窗 / 全局 token）与「有效感受野」的口径、线性注意力的乘法重排、S4 → Mamba → Mamba-2 的选择性与 SSD、Jamba / Zamba 的混合架构、Needle in a Haystack 与 Lost in the Middle
- 15 · [[15-多模态 LLM|多模态 LLM]] —— Vision Encoder + Projector + LLM 三件套、CLIP 的对比学习与 patch 切分、LLaVA 的两阶段与「只训桥」、动态分辨率与视觉 token 预算、Perceiver Resampler / Q-Former 的压缩、原生多模态 vs 拼接式、视频的 token 量级约束

## 相邻专栏

- [[00-机器学习专栏导览|机器学习]] —— 在 `../ml/`。本专栏讲大模型本身，模型训练之外的经典机器学习在那边
- [[00-Agent 专栏导览|Agent]] —— 在 `../agent/`。模型之外的交互与工程：Prompt / Context / Harness、规则边界、评测闭环

## 阅读顺序

01 → 03 是一条架构演进线：先看 Transformer 之前的模型为什么不够用，再看 Transformer 怎么解决，最后看为什么主流大模型只保留解码器。**09 是这条线的补完**——位置信息怎么注入、这件事怎么决定了模型能处理多长的输入。

04 → 08 是「怎么用、有什么边界、怎么选」：采样参数决定怎么从分布里取词，分词决定输入被切成什么，缩放法则解释能力从哪来，幻觉说明它会在哪里不可靠，选型把前面几条收成一张决策表。

**08 之前先读 06 和 07**——能力涌现决定选型的下限，幻觉缓解手段决定能不能降低对模型自身准确性的依赖。

**11 → 13 是 Transformer 内部组件的深入篇**，与 02 是「总览 vs 分件」的关系：02 给出整机骨架，11/12/13 分别把 Attention、FFN、归一化与残差拆到算子与显存层面。**要动模型结构或写 kernel 时就往这三篇走**（配套的算子实现见 `../infra/cuda/`）。

**10 vs `../infra/infer/`**：10 算的是 KV Cache 的**显存账本与注意力布局**（模型侧）；`infer/` 讲的是**引擎怎么把这块显存管起来**（块表、引用计数、抢占）。同一个对象两个层次。

**14 与 09 是一条线**：09 讲位置编码怎么外推（PI / NTK-aware / YaRN / Dynamic NTK），14 接着讲位置之外的两件事 —— **注意力结构本身能不能降到线性**（稀疏 / 线性注意力 / SSM），以及**长上下文到底怎么算有效**（NIAH / RULER / Lost in the Middle）。系统侧的跨设备切分在 `../infra/train/11-长序列训练与上下文并行`。

**15 是唯一一篇跨模态的**：它把文本之外的信息接进同一套自回归框架，核心机制仍是 token 化（图像切 patch、音频切帧）与注意力。视频理解实际依赖 14 的长上下文能力。
