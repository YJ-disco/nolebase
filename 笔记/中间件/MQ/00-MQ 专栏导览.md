---
tags:
  - 中间件/MQ
---

# MQ 专栏导览

本专栏收录消息队列的机制与实现细节。

## 目录

- 01 · [[01-Kafka 知识汇总|Kafka 知识汇总]] —— Kafka 的三大角色（消息系统 / 存储系统 / 流式处理平台）、Topic-Partition-Offset 模型、ISR 与 HW/LEO 的推导、生产者的主线程与 Sender 线程、RecordAccumulator 结构、落盘与发送路径上的 mmap 与 sendfile；然后是**日志存储**整块（分区目录与段命名、滚动与按段删除的粒度、**日志恢复的两种损坏（截断与 inode 先于数据块落盘而多出垃圾）**、**记录格式三代演变 V0/V1/V2（V2 的 RecordBatch 逐字节表 + attributes 位分配）**、压缩与"空批次"的由来、**两级稀疏索引的定位链路**、顺序写与页缓存的账、`sendfile` 省掉哪三次拷贝及**启用 SSL 时失效**、fsync 的取舍与 ISR 如何补回来、以及**官方文档在 IO 调度器上的沉默**）；再往后是**消费者**整块（消费组与分区分配、三种订阅方式与互斥状态、拉模式与 poll 循环、位移提交的自动/手动两条路径与各自的失败方向、seek 与按时间回溯、再均衡的两阶段协议与"组 leader 算分配"、心跳与三组超时、多线程的三种模型），以及**消费者参数逐条**（20 个 `####`，默认值取自官方文档）；中间是 **2011 年原始设计（NetDB'11）** 那一层：**为什么企业级消息系统与专用日志聚合器都不合用**（四条特性错配 + push vs pull）、**单分区上的三个取舍**（segment 追加 + 刷盘后才可见 / 无显式 message id 而用逻辑 offset 寻址 / 不进程内缓存而交给 page cache）、`sendfile` 省掉的是哪两次拷贝、**无状态 broker 用时间 SLA（典型 7 天）替代"知道谁消费完了"**、以及**回退重放**这个"违反队列契约"却被当成特性的能力、**consumer group 的两个决策与 ZooKeeper 四张注册表**（含为何 ownership 是 ephemeral 而 offset 是 persistent）、**rebalance 的对半切分与竞争处理**、交付保证（at-least-once 的重复来自"未干净关闭"）与**当时还没有副本复制**这件事，以及 LinkedIn 的部署/审计与对 ActiveMQ、RabbitMQ 的实测（**50,000 / 400,000 vs 数量级差**、**每条消息开销 9 字节 vs 144 字节**、消费者 **22,000 条/秒**）

## 阅读顺序

目前 1 篇，**76,162 字符 / 92 个标题 / 单节均值 800 字**（对齐 `AGENTS.md` 5.3 的详尽度标准：单节均值 800–1,000，且 ≥2000 字的节有 11 个）。按「概念 → 客户端 → 存储 → 可靠性」的顺序通读：先看**概念与模型**（简介 / 基本概念 / ISR 机制），再看**生产者与消费者**两个客户端（各自的参数逐条），然后是**日志存储**这一层（记录格式 V0/V1/V2、索引、磁盘与零拷贝），最后是**可靠性实现**（失效副本判据、ISR 伸缩、Leader Epoch、读写分离的取舍、三种同步机制对照、leader 选举与 unclean）。再往后是**分区管理**（优先副本的均衡诊断）、**服务端的时间轮**（延迟操作的两条完成路径、层级时间轮、O(1) 摘除）与**客户端的幂等与事务**（三档交付保证、精确一次的边界）。中间夹着 **2011 年原始设计**那一节，用来说明每个机制是为什么长出来的 —— 建议读完概念后回看它。
