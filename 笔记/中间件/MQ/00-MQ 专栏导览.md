---
tags:
  - 中间件/MQ
---

# MQ 专栏导览

本专栏收录消息队列的机制与实现细节。

## 目录

- 01 · [[01-Kafka 知识汇总|Kafka 知识汇总]] —— 三大角色与 Topic-Partition-Offset 模型、生产者与消费者的完整链路、日志存储与索引，以及 2011 年原始设计的三个取舍

## 阅读顺序

目前 1 篇，**76,162 字符 / 92 个标题 / 单节均值 800 字**（对齐 `AGENTS.md` 5.3 的详尽度标准：单节均值 800–1,000，且 ≥2000 字的节有 11 个）。按「概念 → 客户端 → 存储 → 可靠性」的顺序通读：先看**概念与模型**（简介 / 基本概念 / ISR 机制），再看**生产者与消费者**两个客户端（各自的参数逐条），然后是**日志存储**这一层（记录格式 V0/V1/V2、索引、磁盘与零拷贝），最后是**可靠性实现**（失效副本判据、ISR 伸缩、Leader Epoch、读写分离的取舍、三种同步机制对照、leader 选举与 unclean）。再往后是**分区管理**（优先副本的均衡诊断）、**服务端的时间轮**（延迟操作的两条完成路径、层级时间轮、O(1) 摘除）与**客户端的幂等与事务**（三档交付保证、精确一次的边界）。中间夹着 **2011 年原始设计**那一节，用来说明每个机制是为什么长出来的 —— 建议读完概念后回看它。
